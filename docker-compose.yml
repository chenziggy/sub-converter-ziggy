version: '3.8'

services:
  sub-converter-ziggy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "25502:25502"
    volumes:
      - .:/
    environment:
      - NODE_ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge